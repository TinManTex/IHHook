//windowsapi.h
//tex just the basic windows headers, would have like to have called it winapi, but the lua c module has that name
#pragma once

#include <SDKDDKVer.h>
#define WIN32_LEAN_AND_MEAN
#include <windows.h>
